# A133 — the Metal compiler check, measured on real machines (2026-09-15T13:27:05Z)

## node1 (component installed by this audit, 2026-09-15)
$ tools/check-metal.sh
  Apple metal version 32023.921 (metalfe-32023.921.6)
  exit=0

## node2
$ xcrun --find metal        # a path even where the component is absent: not a check
/var/run/com.apple.security.cryptexd/mnt/com.apple.MobileAsset.MetalToolchain-v27.1.266.1.UUz4vC/Metal.xctoolchain/usr/bin/metal
  exit=0
$ bash /tmp/check-metal.sh   # the audit's check: it runs the compiler
Apple metal version 32023.921 (metalfe-32023.921.6)
  exit=0

## node3
$ xcrun --find metal        # a path even where the component is absent: not a check
/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/metal
  exit=0
$ bash /tmp/check-metal.sh   # the audit's check: it runs the compiler
This Mac cannot compile Metal kernels, and this package contains them: the build stops on the
first one mlx-swift compiles, with an error that names the .metal file rather than the cause.

  error: error: cannot execute tool 'metal' due to missing Metal Toolchain; use: xcodebuild -downloadComponent MetalToolchain

Xcode 27 does not ship the Metal compiler; it is a separate component. Install it once:

  xcodebuild -downloadComponent MetalToolchain

That is about 840 MB, and it needs the full Xcode — the command line tools cannot install it. If
Xcode is not installed yet, install it from the App Store first, then run the command above.
  exit=1
