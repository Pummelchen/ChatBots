# A171 — a late-arriving snapshot on the page (2026-09-15T15:03:58Z)

## 1. before: the page applied whatever arrived last
$ git show HEAD:web/app.js | grep -n 'function apply' -A 3
  135:  function applyDevice() {
  136-    const body = document.body;
  137-    let device = screenInfo.detected;
  138-    let layout;
  --
  552:  function apply(next) {
  553-    const first = state.snapshot === null;
  554-    state.snapshot = next;
  555-    // A seat count change replaces the panes, so it is handled before anything draws.
$ git show HEAD:web/app.js | grep -c isStale
  0
$ git show HEAD:web/deltas.js | grep -c isStale
  0   (the rule did not exist on the page either)
## 2. after
$ grep -n 'isStale' web/app.js web/deltas.js | head -4
  web/app.js:556:    if (window.ChatBotsDeltas.isStale(next, state.snapshot)) return;
  web/deltas.js:4:// folded into the live entries (`applyDelta`), and which of two snapshots is newer (`isStale`). They
  web/deltas.js:85:  function isStale(next, current) {
  web/deltas.js:98:  const api = { applyDelta: applyDelta, emptyLive: emptyLive, isStale: isStale };
$ node tools/check-web-deltas.js   # before (the page does not call the rule)
    ok    an unorderable pair is not treated as stale
    FAIL  every snapshot the page applies goes through the guard
  1 deltas check(s) failed
$ node tools/check-web-deltas.js   # after
    ok    every snapshot the page applies goes through the guard
  all deltas checks passed
