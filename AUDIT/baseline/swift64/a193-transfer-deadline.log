the servers this probe runs:
  stalled      10 bytes of a declared 1 MB body, then silence
  slow         100 bytes a second for 10s (finishes inside the deadline's window)
  long slow    2 KiB a second for 35s (outlasts the window, stays above the floor)
  flaky        the first connection stalls, every later one serves 2000 bytes

the stall, before and after
  ok    without a deadline, a stalled transfer is still running after 12s (the hang)
  ok    with the shipped deadline the same stall fails instead of hanging
  ok    and curl says why

the counterweights — a slow transfer is not the same as a stalled one
  ok    100 B/s that finishes inside the window completes
  ok    2 KiB/s outlasting the 30s window completes (a floor on speed, not a cap on time)
  ok    the stalled attempt is abandoned, retried and the download completes

the three scripts, as they now write it
  ok    install.sh defines the deadline once and uses it in both downloads
  ok    tools/fetch-metal.sh carries it on its download
  ok    tools/fetch-audit-tools.sh carries it in fetch()
  ok    the values measured above are the ones the scripts ship

mutations — each one has to be caught
  ok    M1 the check sees a download that lost the deadline
  ok    M2 the check sees one option missing
  ok    M3 an over-aggressive deadline loses the slow-transfer counterweight

all A193 probe checks passed
