# A176 — unreferenced code in the app target (2026-09-15T15:28:29Z)

## 1. before: declarations with no callers
$ git grep -n 'EndpointBar\|ScaledLength' HEAD -- Sources Tests
  HEAD:Sources/ChatBotsApp/Views/AgentHeader.swift:290:struct EndpointBar: View {
  HEAD:Sources/ChatBotsApp/ZoomStore.swift:107:struct ScaledLength: ViewModifier {

  EndpointBar: its onEdit closure had no caller, its draft and showingEditor were never read,
  and its @EnvironmentObject ThemeStore would have trapped had it been instantiated without one.
  ScaledLength: a ViewModifier with no extension method and no call site.

## 2. after: gone, and nothing referenced them
$ grep -rn 'EndpointBar\|ScaledLength' Sources Tests
  (no matches)
$ swift build --build-tests
  Build complete! (1,17 secs)
$ swift test
  ✔ Test run with 882 tests in 153 suites passed after 20.142 seconds.
  ✔ Test run with 10 tests in 3 suites passed after 0.015 seconds.

## 3. and the linters moved the right way
  swiftlint 255 -> 254 (the deleted region carried a vertical_whitespace finding)
  authored swift-format 743 -> 742
