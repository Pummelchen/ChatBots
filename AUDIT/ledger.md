# Audit ledger

Generated from `AUDIT/ledger.json` by `AUDIT/render-ledger.py`. Do not edit by hand.

- total: 68
- done: 0
- open: 68
- blocked: 0

## Open by severity

| severity | open | total |
| --- | --- | --- |
| S0 | 1 | 1 |
| S1 | 3 | 3 |
| S2 | 34 | 34 |
| S3 | 30 | 30 |

## By tier

| tier | total |
| --- | --- |
| A | 68 |

## Tasks

| id | sev | tier | project | file:line | title | category | status | host | discovered-by | evidence-before | fix-summary | evidence-after | commit | blocked-reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AUDIT-0001 | S0 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DeviceProfiles.swift:242; Sources/ChatBotsCore/HTTP/APIServer.swift:218 | Unauthenticated /api/device?h=Int.max overflows the match arithmetic and aborts the engine process | logic/remote-dos | OPEN | Node1.local | subagent-attachments | APIServer.swift:218-222 reads w and h from the query with no upper bound (HTTPRequest.int = Int.init). DeviceProfiles.nearest (DeviceProfiles.swift:241-245) computes abs(lhs.height - height) * 1000 + ... for every candidate; with h=9223372036854775807 and a candidate height of 812 the sum exceeds Int.max and Swift traps, aborting the process. GET /api/device is routed before the engine dispatch and needs no body, so any LAN client can crash the running engine. |  |  |  |  |
| AUDIT-0002 | S1 | A | ChatBotsCore | Sources/ChatBotsCore/Models/ModelStore.swift:113-120 | modelID from POST /api/seat escapes models/ by absolute path or .. and is loaded as a checkpoint | path-traversal | OPEN | Node1.local | subagent-persistence | APIServer+Commands.swift:57 seatChange takes body.modelID; EngineService.swift:218-221 passes ModelCatalog.resolve(modelID) (unchanged for a non-catalogue id) to engine.setModel; MLXEngine.swift:180 calls ModelStore.localCheckpoint(for: spec.modelID). localCheckpoint builds candidates with URL(fileURLWithPath: modelID) for an absolute id and root.appending(path: modelID) otherwise, with no containment check, then isCompleteCheckpoint() accepts any directory holding config.json, tokenizer.json and a .safetensors. A LAN client can make the engine load a checkpoint from anywhere on disk. Trigger: POST /api/seat {"seat":"Agent 1","modelID":"/tmp/evil"}. |  |  |  |  |
| AUDIT-0003 | S1 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/ConversationEngine+Transcript.swift:79 | The only production caller discards ConversationStore.save's failure, so a lost save is silent | data-loss | OPEN | Node1.local | subagent-persistence | ConversationStore.save returns Bool and is @discardableResult (ConversationStore.swift:126-127); the sole caller is saveConversation() which does `_ = store.save(record)` (ConversationEngine+Transcript.swift:79). On a full disk, a permission failure, or an undecodable index (readAll() nil, line 148) every turn returns false and nothing observes it; the UI shows the conversation normally and it is gone at quit. isUnreadable (line 277) has no production caller. |  |  |  |  |
| AUDIT-0049 | S1 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIResponsesEngine.swift:161-178,208-214 | The /v1/models probe skips endpointRefusal and follows redirects on URLSession.shared while carrying the API key | ssrf | OPEN | Node1.local | subagent-openai | availableModels() builds its URL with endpointURL() (208-214), which never calls OpenAIEndpoint.endpointRefusal, and fetches with URLSession.shared (line 171), which follows redirects and has no delegate. It attaches `Authorization: Bearer <effectiveAPIKey>` (lines 168-170) and echoes up to 300 bytes of a non-2xx body into the error (174-177) that reaches the transcript and /api/state. The generation path does refuse link-local and pins a NoRedirects delegate (OpenAIModels.swift:84, OpenAIResponsesClient.swift:20-28,48-52); the probe has neither, so a seat baseURL of http://169.254.169.254 or a 302 from a hostile endpoint is fetched and reflected. |  |  |  |  |
| AUDIT-0004 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DocumentImport.swift:240 | Image pixel-count cap is bypassed for PNG/JPEG/GIF/WebP, which pass through and are decoded later | resource-exhaustion | OPEN | Node1.local | subagent-attachments | DocumentImport.wireRepresentation returns early when AttachedDocument.mediaType(of: data) != nil (line 240), before validateImage() and its maximumImagePixels guard (line 222). The comment at Attachments.swift:299-301 claims those images are never decoded, but TurnLoop.swift:401 (CIImage(data:)) and MLXEngine.swift:344-345 decode exactly those bytes. A sub-64 MB PNG declaring 100000x100000 (or GIF 65535x65535) is accepted and decoded. |  |  |  |  |
| AUDIT-0005 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/Attachments.swift:310 | No aggregate attachment byte bound: 24 x 64 MB are retained and base64-amplified every turn | resource-exhaustion | OPEN | Node1.local | subagent-attachments | defaultMaximumFileBytes = 64 MB (Attachments.swift:310) and maximumAttachments = 24 (EngineServiceAttachments.swift:30) are the only bounds; no total-bytes cap exists. ConversationEngine+TurnLoop.swift:169 re-sends attachments to the engine every turn and OpenAIResponsesEngine.swift:230-231 base64-encodes every image into a data URL. Up to ~1.5 GB of uploads, copied several times, on an 8 GB Mac. |  |  |  |  |
| AUDIT-0006 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/TranscriptWriter.swift:69,85 | Transcript interpolates topic and speaker name without stripping newlines, allowing forged entries | injection | OPEN | Node1.local | subagent-persistence | TranscriptWriter writes "Topic: \(topic)\n" (line 69) and "[timestamp] \(turn.speakerName.uppercased())\n" (line 85) with no control-character sanitation. EngineService.swift:153-161 accepts any non-blank topic, and seat names are trimmed with .whitespaces (which excludes newline). A LAN client can POST a topic containing a newline plus a fabricated "[timestamp] OTTO" line; Save Conversation then exports a transcript containing the forged entry. |  |  |  |  |
| AUDIT-0007 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/ConversationStore.swift:303 | deleteAll removes only the index and swallows failure; a stranded .tmp resurrects deleted conversations | data-loss | OPEN | Node1.local | subagent-persistence | deleteAll is `try? FileManager.default.removeItem(at: indexURL)` (line 304). readAll() falls back to readTemporary() (lines 249-253, 264-272) when the index is absent. A crash between write() and replaceItemAt leaves conversations.json.tmp; deleteAll leaves it behind, so the next load() returns the conversations the user deleted, and a failed removeItem is silent. |  |  |  |  |
| AUDIT-0008 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/ConversationStore.swift:264-272 | readTemporary reports an unreadable temporary as an empty history and isUnreadable never inspects it | data-loss | OPEN | Node1.local | subagent-persistence | readTemporary() returns [] when the .tmp exists but readIndex() fails, while readAll()'s contract says nil means "exists but cannot be decoded"; save() (line 148) then treats it as empty and overwrites the only copy. isUnreadable (line 277) only checks indexURL, so in exactly the crash state readTemporary exists for it reports false. |  |  |  |  |
| AUDIT-0009 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Models/ModelStore.swift:91-94 | prepare() swallows directory creation and still points HF_HUB_CACHE at the failing path | logic | OPEN | Node1.local | subagent-persistence | `try? FileManager.default.createDirectory(at: root, ...)` (line 91) discards the error, then HF_HUB_CACHE is set to root.path (lines 92-94) and setenv's result is unchecked. A read-only parent or a regular file at root makes every later download fail with an unrelated hub error. |  |  |  |  |
| AUDIT-0010 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAICompatibility.swift:167-171 | .secrets.env is read with no permission check and nothing creates it 0600 | secret-handling | OPEN | Node1.local | subagent-persistence | BuiltInKeys reads .secrets.env with String(contentsOf:) and no mode inspection (OpenAICompatibility.swift:167-171). grep finds no chmod for it anywhere in the repo, unlike CertificateStore.swift:306/320/326 which applies 0o600 to the TLS key. A key file created under the usual umask is 0644 and read silently. |  |  |  |  |
| AUDIT-0011 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Research/TavilyClient.swift:78,234 | Tavily URLSession follows redirects while carrying the API key; no redirect-refusing delegate | secret-handling | OPEN | Node1.local | subagent-research | TavilyClient builds `URLSession(configuration:)` with no delegate (line 78) and sends `Authorization: Bearer \(apiKey)` (line 234). OpenAIResponsesClient.swift:20-26 installs a NoRedirects delegate with the comment that following a redirect is how a validated URL reaches a host that never was; TavilyClient has no equivalent, so a 302 from api.tavily.com is followed with the key attached. |  |  |  |  |
| AUDIT-0012 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/ConversationEngine+TurnLoop.swift:248-249,277-278 | Searches are charged only when a turn produces non-empty text, so tool-only turns spend for free | resource-leak | OPEN | Node1.local | subagent-research | The only call to conversation.research?.record(searchCount:addedSomething:) sits inside the else branch of `if clean.isEmpty` (ConversationEngine+TurnLoop.swift:248-249, 277-278). A turn whose rounds emit only tool calls ends with empty text (Engine/TurnLoop.swift:213-218), so searches and rounds stay 0 and budgetRemaining never drops; the maxSearches ceiling can be bypassed up to configuration.maxTurns (default 40). |  |  |  |  |
| AUDIT-0013 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/TurnLoop.swift:138,245-250 | No per-turn cap on tool calls: 3 rounds x N calls can overshoot maxSearches arbitrarily | resource-leak | OPEN | Node1.local | subagent-research | maxToolRounds = 3 (TurnLoop.swift:138) caps rounds, and each round dispatches the whole collected list (`for call in toolCalls`, lines 245-250). The search ceiling is checked once before the turn (ConversationEngine+TurnLoop.swift:190-195), so one turn with many <tool_call> blocks spends many billed Tavily calls. |  |  |  |  |
| AUDIT-0014 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Research/WebTools.swift:96-105 | fetch_page forwards any http(s) host, including loopback/private/link-local, to the extractor | ssrf | OPEN | Node1.local | subagent-research | readableURL accepts any http(s) URL with a non-empty host and no deny-list or resolution check (WebTools.swift:96-105); WebToolValidationTests.swift:30-31 blesses loopback. FetchPageTool delegates the fetch to Tavily (TavilyClient.extract), so a model or injected content can send Tavily to internal addresses such as http://169.254.169.254/latest/meta-data/ or http://2130706433/ and the content returns to the model. Contrast OpenAIModels.swift:101-111, which refuses link-local hosts for model endpoints. |  |  |  |  |
| AUDIT-0015 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Research/WebTools.swift:41-47,121-128; Sources/ChatBotsCore/Engine/TurnLoop.swift:309 | Fetched web content enters the prompt undelimited and uninspected for instruction smuggling | prompt-injection | OPEN | Node1.local | subagent-research | WebSearchTool concatenates hit.title/url/content and FetchPageTool returns "Content of \(page.url) (\(page.title)):\n\n\(page.content)" with no fence or untrusted marker; TurnLoop.swift:309 inserts the string verbatim as a role:"tool" entry. A fetched page can therefore inject instructions into the running model, which is allowed to invoke further privileged tools. Newlines in a hostile page title also forge transcript lines in the synthesis prompt (ConversationEngine+Research.swift:44-47, ConversationEngine+TurnLoop.swift:311). |  |  |  |  |
| AUDIT-0016 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Research/ResearchReporting.swift:113-114 | synthesisPrompt interpolates the API-settable topic and display names raw, including the transcript boundary token | prompt-injection | OPEN | Node1.local | subagent-research | "The question was: \(question)" and "The analysts were: \(participants.joined(separator: ", "))" (ResearchReporting.swift:113-114) take conversation.topic and seat displayName (ConversationEngine+Research.swift:50-54) with no neutralisation, unlike fencedTranscript() a few lines later. Both are settable through the unauthenticated API, so a topic or seat name can forge the "===== BEGIN SESSION TRANSCRIPT" boundary and inject instructions ahead of the real fence. |  |  |  |  |
| AUDIT-0017 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Conversation/ConversationStore.swift:136-137,189 | Conversation history is created with the process umask (0755 dir / 0644 file) rather than private modes | secret-handling | OPEN | Node1.local | subagent-persistence | ConversationStore creates its directory (line 136) and writes conversations.json (line 189) with default permissions. Every kept conversation (topic and full transcript, possibly including cloud-seat content) is therefore world-readable on a multi-user Mac, unlike the TLS key which CertificateStore creates 0600. |  |  |  |  |
| AUDIT-0018 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/HTTP/APIServer.swift:77-101; Caddyfile | The whole /api surface is unauthenticated and, through Caddy, reachable from the LAN by design | authz | OPEN | Node1.local | human | crossOriginRefusal (APIServer.swift:77-101) blocks cross-origin browser requests and requires application/json, but there is no authentication or authorization anywhere on the routes: any client that can reach the port can read every conversation, drive every control and upload attachments. SECURITY.md documents this as deliberate (the LAN phone use case) and AGENTS.md repeats it; Caddyfile carries no host so it listens on every interface. This is recorded so the risk is explicit rather than hidden, not as an unnoticed defect. |  |  |  |  |
| AUDIT-0029 | S2 | A | repo | tools/mac-checks.sh:172-207; tools/analysis-waivers.txt | SwiftLint and swift-format are enforced against recorded waiver counts, not --strict, and force_unwrapping is never enabled | tooling/standard | OPEN | Node1.local | human | mac-checks.sh runs `swiftlint lint --quiet` and `swift-format lint` and compares the finding count to a waiver (217/444) in tools/analysis-waivers.txt; .swiftlint.yml has no opt_in_rules, so force_unwrapping is not on. The brief requires both tools run with --strict so linter warnings fail the build. Baseline: swiftlint 215 findings, swift-format 439 diagnostics. |  |  |  |  |
| AUDIT-0030 | S2 | A | repo | tools/; pyrightconfig.json | Python has no committed Ruff configuration, so B/E722/S101/PT and datetime/encoding rules are not enabled | tooling/standard | OPEN | Node1.local | human | There is no pyproject.toml, ruff.toml or setup.cfg; `ruff check tools/` therefore runs the default select (E4, E7, E9, F). flake8-bugbear (B), flake8-bandit (S101), flake8-pytest-style (PT), flake8-datetimez (DTZ) and unspecified-encoding are not selected, so the pitfalls the brief names are not covered by the tool. |  |  |  |  |
| AUDIT-0031 | S2 | A | repo | web/ | JavaScript has no committed formatter or linter and no pinned toolchain | tooling/standard | OPEN | Node1.local | human | web/ holds 11 browser-facing JS modules on the LAN-served page and there is no package.json, no eslint/prettier config and no npm dependency scanner. The only JS checks are the two hand-written behavioural rules in tools/check-web-deltas.js and tools/check-web-votes.js; nothing checks syntax, escaping or unused bindings across the other nine modules. |  |  |  |  |
| AUDIT-0032 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportClient.swift:15-16; TransportCheck.swift:130-132 | The client cannot observe the peer certificate, so the documented 'mismatch is visible' claim is false | cert-validation | OPEN | Node1.local | subagent-transport | WebTransportClient.swift:15-16 says the fingerprint is reported and logged 'so a mismatch is visible even though it is not enforced', but the client uses trustPolicy .localDevelopmentSelfSigned, whose library path sets tls.peerAuthentication(.none); WebTransportNetworkSession exposes no peer-certificate accessor. TransportCheck.swift:130-132 reports the local file's fingerprint while claiming it is 'the one on the other end of the socket', and Checks.swift:37 loads a local identity that is never used. An operator who reads the note believes an impersonating loopback peer is detectable; by construction it is not. |  |  |  |  |
| AUDIT-0033 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportClient.swift:204-208,255-271; WebTransportClientReader.swift:165-169 | A reconnecting client cancels but never awaits the old reader, which can poison the new session's shared state | concurrency | OPEN | Node1.local | subagent-transport | teardown() only calls readerTask?.cancel() and clears the single client-wide readerError/pendingReplies/eventContinuation, without awaiting the task. The reader checks cancellation only at the top of its outer loop (WebTransportClientReader.swift:28), so a chunk already returned by receive() is fully processed. connect() then installs a new readerTask/eventContinuation (:196-208). The old reader waking later calls failReader, which finishes the new greeting's continuation and sets the shared readerError, so the new connect fails with the old error (send at :333-335). Triggered by the reconnect path at :150-152. |  |  |  |  |
| AUDIT-0043 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/MLXEngine.swift:253-257; ChatModels.swift:179; TurnSettings.swift:42-45 | A checkpoint-declared context window is accepted unvalidated and can overflow the generation cap | overflow | OPEN | Node1.local | subagent-engine | contextWindowFor reads max_position_embeddings/max_sequence_length and accepts any positive Int (lines 253-257), which becomes settings.contextWindow. With thinking .unlimited, ChatModels.swift:179 returns answerBudget + max(highHeadroom, contextWindow) and TurnSettings.generationCap adds 4096, so a config.json declaring a value near Int.max traps in TurnSettings.swift:42-45, reached from TurnLoop.swift:100. A model id reaches the hub with no allow-list. |  |  |  |  |
| AUDIT-0050 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIModels.swift:108 | Link-local refusal is a string prefix and misses IPv4-mapped IPv6 literal forms | ssrf | OPEN | Node1.local | subagent-openai | endpointRefusal refuses host.hasPrefix("169.254.") \|\| hasPrefix("fe80:") \|\| hasPrefix("[fe80:"). Foundation strips the brackets from an IPv6 literal, so http://[::ffff:169.254.169.254]/ yields host ::ffff:169.254.169.254 and passes; http://[0:0:0:0:0:ffff:a9fe:a9fe]/ likewise. The [fe80: branch is dead for url.host. The accepted URL then also passes into the streaming client. |  |  |  |  |
| AUDIT-0051 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIResponsesClient.swift:199-203 | CHATBOTS_TRACE_API=off turns the conversation trace ON | secret-handling | OPEN | Node1.local | subagent-openai | `return !(lowered.isEmpty \|\| lowered == "0" \|\| lowered == "false" \|\| lowered == "no")` — off, disabled, none and OFF are all outside the off-list, so traceRequest writes up to 2000 characters of instructions and conversation to stderr. SECURITY.md documents that same list as the off-list, so 'off' silently enables it; TraceSwitchTests.swift:60-62 pins 'anything' as on. |  |  |  |  |
| AUDIT-0052 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIResponsesClient.swift:235 | The trace prints the request URL verbatim, including any credential embedded in it | secret-handling | OPEN | Node1.local | subagent-openai | `let header = "[trace] POST \(url.absoluteString) (...)"` prints the full URL. SECURITY.md promises the trace does not print the API key; true for the Authorization header, but a base URL such as https://user:sk-...@api.deepseek.com or https://host/v1?api_key=sk-... is reproduced in full. |  |  |  |  |
| AUDIT-0053 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIResponsesClient.swift:268-271,287-294 | SSE response size is unbounded; the 600-character cap is applied after the line is buffered | resource-exhaustion | OPEN | Node1.local | subagent-openai | `for try await line in bytes.lines { detail += line; if detail.count > 600 { break } }` buffers a full line before the cap runs, and the success path feeds an unbounded data: payload to JSONSerialization.jsonObject (line 292). There is no byte cap and no Content-Length check; timeouts are 600 s/3600 s. |  |  |  |  |
| AUDIT-0054 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAICompatibility.swift:28-35 | Parameter-set inference uses substring matching where key routing deliberately uses exact host equality | logic | OPEN | Node1.local | subagent-openai | `lowered.contains("api.openai.com") \|\| contains("openai.azure.com") \|\| contains("openrouter.ai")` makes https://api.openai.com.evil.test or https://evil.test/?x=api.openai.com infer .strict, silently dropping top_k/min_p/repetition_penalty. BuiltInKeys.key (OpenAICompatibility.swift:196-206) explicitly rejects contains for hosts. |  |  |  |  |
| AUDIT-0055 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIModels.swift:78-86 | A base URL with a query or fragment silently loses the request path | logic | OPEN | Node1.local | subagent-openai | `guard let url = URL(string: trimmed + path)` concatenates text. https://host.example#frag becomes https://host.example#frag/v1/responses (empty path), and https://host?x=1 puts the path into the query, so the POST targets / instead of /v1/responses. |  |  |  |  |
| AUDIT-0056 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/OpenAI/OpenAIResponsesClient.swift:291-294 | Malformed SSE data lines are silently skipped, so a truncated answer can be reported as complete | parsing | OPEN | Node1.local | subagent-openai | `guard let event = try? JSONSerialization.jsonObject(with: Data(payload.utf8)) as? [String: Any] else { continue }` drops an undecodable data: line with no notice; a later well-formed response.completed finishes the turn with whatever text survived, indistinguishable from a complete answer. |  |  |  |  |
| AUDIT-0057 | S2 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/EngineService.swift:210-213; ConversationEngine+Seats.swift:83-97,170-174 | Changing a seat's baseURL or API key is reported by the snapshot but never reaches the live client | facade | OPEN | Node1.local | subagent-openai | engine.updateSeat(spec) stores the new baseURL/apiKey on the seat but only pushes displayName/persona/thinking to the live engine; ConversationEngine.setEndpoint(_:for:) has no callers, and OpenAIResponsesEngine.spec is a let with a cached client. POST /api/seat (unauthenticated) and the API sheet therefore show an endpoint change that does not take effect until the engine process restarts. This also blocks the obvious remote repoint-then-exfiltrate chain, so it is a facade rather than an active hole. |  |  |  |  |
| AUDIT-0058 | S2 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:87-90,280-309; ChatControllerSeatConfiguration.swift:118-129 | The app adopts any process answering on 7790 without verifying it, then hands it every Keychain API key | secret-handling | OPEN | Node1.local | subagent-app | start() does `if await isEngineAnswering() { state = .running(owned: false); return }`; the probe uses WebTransportEngineClient with .localDevelopmentSelfSigned, which does not verify the peer. ChatBotsApp.swift:115 then calls controller.applyAPIEndpoints, and APIEndpointStore.swift:53-58 fills apiKey from the Keychain and sends it via .updateSeat. A same-user process that binds 127.0.0.1:7790 receives the user's cloud keys and can inject snapshot.error/notices into the UI. Precondition is local code execution as the user, which is exactly what the Keychain boundary is meant to contain. |  |  |  |  |
| AUDIT-0059 | S2 | A | ChatBotsApp | Sources/ChatBotsApp/ChatControllerControls.swift:95-107,72-75; ChatControllerSeatConfiguration.swift:92-98 | An engine refusal is treated as acceptance, clearing the moderator draft and showing a refused checkpoint as applied | logic | OPEN | Node1.local | subagent-app | deliver() does `_ = try await client.send(request); return true`. A .refused reply is a normal reply (only .failed throws), so EngineService.swift:171-173 refusing an over-2000-character steer still returns true; deliverModeratorDraft then clears the box (draft(afterSendOf:accepted:true:)) and discards the reason, and setModel sets pane.spec.modelID for a checkpoint the engine refused. The tests that claim to pin both only cover the no-client case. |  |  |  |  |
| AUDIT-0060 | S2 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:111-127,275; ChatControllerConnection.swift:30-39 | Cancellation-blind retry loops: try? Task.sleep turns a paced poll into a hot spin after the view disappears | concurrency | OPEN | Node1.local | subagent-app | `try? await Task.sleep(for: .milliseconds(400))` swallows CancellationError and neither loop checks Task.isCancelled, so after the window's .task is cancelled the 400 ms pacing is gone and the loop spins (QUIC connect + state + disconnect) for the full 90 s. terminateChild nils self.process before returning, so the cancelled start() cannot see the killed child and finally assigns state = .failed, overwriting the .idle set by shutdown(). ChatControllerConnection.swift:37 has the same shape and can install a client after cancellation. The poll loop at ChatControllerConnection.swift:73-76 does check Task.isCancelled. |  |  |  |  |
| AUDIT-0061 | S2 | A | ChatBotsApp | Sources/ChatBotsApp/ChatControllerAttachments.swift:180-194 | Attachments are read fully into memory on the main thread with no size cap | resource-exhaustion | OPEN | Node1.local | subagent-app | addFiles is @MainActor and does `queued.append((name, try Data(contentsOf: url)))` synchronously, contradicting the doc comment that says the upload runs in the background. There is no size or count check, while the engine caps at 64 MB per file and 24 files, so several large PDFs/TIFFs block the UI and buffer everything before the Task starts. |  |  |  |  |
| AUDIT-0062 | S2 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:204,206,212-220 | The launch path falls back to a world-writable fixed path and inherits the app's whole environment | process-spawn | OPEN | Node1.local | subagent-app | candidates.append(URL(fileURLWithPath: "/usr/local/bin/chatbots-cli")) and `candidates.first { isExecutableFile(atPath:) }` with no signature check anywhere in Sources; process.environment is never set, so the child inherits OPENAI_API_KEY/DEEPSEEK_API_KEY/TAVILY_API_KEY/CHATBOTS_TRACE_API, and then receives every seat API key over the wire. Arguments are built only from a UInt16 port, so there is no argument injection. |  |  |  |  |
| AUDIT-0019 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/VisionSupport.swift:74 | Vision support is decided by substring match on short markers (o3/o4) and the test for it is tautological | logic | OPEN | Node1.local | subagent-attachments | declaresVision uses visionModelMarkers.contains { name.contains($0) } (VisionSupport.swift:74,82) with markers including "o3" and "o4", so any id containing those substrings is declared vision-capable. VisionCapabilityTests.swift:65 asserts `== .unknown \|\| == .supported`, which passes for .supported and cannot detect the failure it is named for. |  |  |  |  |
| AUDIT-0020 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DeviceProfiles.swift:203-204,230-235 | DeviceClass.desktop resolution is dead: `all` holds no desktop profile so desktop viewports return nil | logic | OPEN | Node1.local | subagent-attachments | all = phones + samsung + tablets + androidTablets (line 203) contains only .phone and .tablet entries, but nearest() selects candidateClass .desktop for width >= 1000 or a non-mobile window (lines 230-233) and then filters all by it, so the candidate set is empty and it returns nil (lines 234-235). DeviceProfileTests.swift:121,128 accept nil and cannot detect it. |  |  |  |  |
| AUDIT-0021 | S3 | A | ChatBotsCLI | Sources/ChatBotsCLI/CommandLine.swift:116; Sources/ChatBotsCLI/CLI.swift:52-53 | The Tavily key can be passed on the command line, exposing it in argv and shell history | secret-handling | OPEN | Node1.local | subagent-research | --key stores the value (CommandLine.swift:116) and CLI.swift:52-53 does setenv("TAVILY_API_KEY", key, 1). Any local process can read argv and the shell records it in history; TAVILY_API_KEY and .secrets.env are the safe alternatives and are already supported. |  |  |  |  |
| AUDIT-0022 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DocumentIngestor.swift:111-114 | readBounded swallows a read error with try? and returns a silently truncated file as complete | logic | OPEN | Node1.local | subagent-attachments | `guard let chunk = try? handle.read(upToCount: 64 * 1024), !chunk.isEmpty else { break }` (lines 111-114): a mid-file I/O error yields nil, the loop breaks and the partial bytes are returned as the whole document; the caller only reports the extractor's own truncation flag. |  |  |  |  |
| AUDIT-0023 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DocumentImport.swift:65-66 | Unchecked 0..<document.pageCount on an untrusted PDF would trap on a negative count | logic | OPEN | Node1.local | subagent-attachments | `for index in 0..<document.pageCount` (line 66) with document built straight from caller bytes (lines 61-63) and pageCount unclamped; a negative value traps on the Range precondition. Whether PDFKit can return a negative count is unconfirmed, hence suspected. |  |  |  |  |
| AUDIT-0024 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Attachments/DocumentImport.swift:143-150 | TextutilExtractor ignores the bounded data and re-opens url.path, so its size checks cover a different read | toctou | OPEN | Node1.local | subagent-attachments | The extractor runs /usr/bin/textutil on url.path (lines 143-150) and never uses the `data: Data` parameter, so DocumentIngestor's lstat/regular-file and bounded-read checks (DocumentIngestor.swift:63-68,77) apply to a different read. Exploitability is low (0700 UUID staging dir on the upload path) and SystemProcess's output ceiling still bounds the result. |  |  |  |  |
| AUDIT-0025 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Research/TavilyClient.swift:107-108,131-141 | includeAnswer is decoded then discarded; the retry comment claims the answer is preserved | logic | OPEN | Node1.local | subagent-research | Response declares `let answer: String?` (line 108) but nothing reads it; search returns [SearchHit] only, and the .retryAdvanced branch re-calls with includeAnswer under a comment asserting the answer is no longer dropped. includeAnswer: true remains a no-op. |  |  |  |  |
| AUDIT-0026 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Research/TavilyClient.swift:159-163 | The basic-to-advanced retry makes two billed Tavily calls while the budget charges one | resource-leak | OPEN | Node1.local | subagent-research | outcome(for:depth:) returns .retryAdvanced for an empty basic result and search recurses at .advanced (lines 138-140), so one web_search tool call costs two billed calls against a budget that charges one (ConversationEngine+TurnLoop.swift:234). |  |  |  |  |
| AUDIT-0027 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Research/TavilyClient.swift:254 | Untrusted Tavily response is decoded and mapped with no byte or item-count bound | resource-exhaustion | OPEN | Node1.local | subagent-research | JSONDecoder().decode(Result.self, from: data) (line 254) has no body cap; search maps every result and extract clips only raw_content, leaving title/url and the array itself unbounded. A hostile or compromised upstream can force large allocations before any limit applies. |  |  |  |  |
| AUDIT-0028 | S3 | A | ChatBotsCore | Tests/ChatBotsCoreTests/VisionCapabilityTests.swift:65 | Vision capability test asserts an outcome it claims to forbid | test-quality | OPEN | Node1.local | subagent-attachments | #expect(spec.visionSupport == .unknown \|\| spec.visionSupport == .supported) is named "A plain checkpoint id is not mistaken for a vision model by substring" but passes when the value is .supported, and its comment claims "qwen3-vl" is a substring of "Qwen3.5-4B", which it is not. The test can only fail if declaresVision returns false. |  |  |  |  |
| AUDIT-0034 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportServerSession.swift:163,26-28,42 | Two paths close a session twice, contradicting the code's own 'exactly one closer' invariant | logic | OPEN | Node1.local | subagent-transport | refuseFraming does `try? await session.close(reason:)` and serve's defer then closes the still-owned entry again; the startup watchdog (:42) closes and the defer closes again. WebTransportServer.swift:201-203 states the library's close() must not be called twice. The library's lease release is idempotent so the practical impact looks nil, but the documented invariant is false. |  |  |  |  |
| AUDIT-0035 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportServerSession.swift:50 | A session that cannot open its stream ends with no recorded reason | logic | OPEN | Node1.local | subagent-transport | `guard let stream = try? await session.acceptBidirectionalStream() else { return }` discards the error and records no note(), unlike every neighbouring refusal path (:85, :138, :144, :161). recentSessionErrors/lastSessionError stay empty for this failure, indistinguishable from a clean close. |  |  |  |  |
| AUDIT-0036 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/CertificateStore.swift:337-338,350-374 | The TLS key is world-readable under a permissive umask until a post-hoc chmod, and the run directory is never restricted | crypto | OPEN | Node1.local | subagent-transport | openssl writes -keyout privateKey.path under the process umask and restrictToThisUser(privateKey) chmods afterwards (:374); the code concedes this at :310-315. Under umask 022 the key exists 0644 in the window. The run directory is created with default mode and never verified (tools/start-app.sh:71 is a plain mkdir -p), and permissions(of:) at :328-331 follows symlinks. |  |  |  |  |
| AUDIT-0037 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportServer.swift:204-209 | stop() closes every session serially on the MainActor with the library's 15 s per-session timeout | resource-leak | OPEN | Node1.local | subagent-transport | `for session in liveSessions { try? await session.close() }` runs serially; the configuration never sets timeoutMilliseconds, so each close uses the library default 15000 ms, and up to 16 unresponsive peers can block the main actor for minutes during shutdown. The library's bounded shutdown(gracePeriodMilliseconds:) is not used. |  |  |  |  |
| AUDIT-0038 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportServerSession.swift:89,110 | Per-session receive buffer can be driven to the ~85 MB protocol cap with no aggregate budget | resource-leak | OPEN | Node1.local | subagent-transport | LengthFraming.read only rejects a declared length above ProtocolLimits.maximumMessageBytes (~85 MB); buffer.append(chunk) accumulates per session (line 89) and once one whole frame arrives the session joins spokenSessions (line 110), disarming the startup watchdog. 16 sessions x 85 MB is about 1.3 GB on an 8 GB machine, from a loopback peer. |  |  |  |  |
| AUDIT-0039 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/TransportCheck.swift:317,321 | The installer's green banner reports a canned session count and waits with a fixed sleep instead of a readiness signal | facade | OPEN | Node1.local | subagent-transport | report.sessionCount = 1 is hardcoded (line 321) rather than read from the server the check started, and `try? await Task.sleep(for: .milliseconds(400))` (line 317) is the only wait for the event stream. succeeded does not depend on either, so the banner is not evidence of what it claims. |  |  |  |  |
| AUDIT-0040 | S3 | A | repo | SECURITY.md:66; Sources/ChatBotsCore/Support/RunDirectory.swift:35-39; CertificateStore.swift:107-109 | SECURITY.md's claim that the TLS key lives in .run/ is false for installed builds | docs | OPEN | Node1.local | subagent-transport | RunDirectory.resolve returns .run only when Package.swift is found; otherwise ~/Library/Application Support/ChatBots. The released app runs from ~/Applications, so its private key lives in Application Support (and in Time Machine copies). CertificateStore.swift:107-108 also says 'Defaults to the project's .run' although directory is a required parameter with no default. |  |  |  |  |
| AUDIT-0041 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/SystemProcess.swift:191 | Unchecked fcntl(F_SETFL) can defeat the deadline the certificate path relies on | native-memory | OPEN | Node1.local | subagent-transport | `_ = fcntl(descriptor, F_SETFL, flags \| O_NONBLOCK)` discards the result. If it fails, drain()'s non-blocking assumption is broken, read() blocks, collectOutput's deadline is unreachable, and CertificateStore's openssl invocation can wedge engine start instead of timing out. |  |  |  |  |
| AUDIT-0042 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Transport/WebTransportServerSession.swift:63-69,77,166-168 | Two tasks write the same stream; the claimed single serialization point is not enforced by a primitive | concurrency | OPEN | Node1.local | subagent-transport | The per-session writer task (:63-68) and the initial state send (:77) both call send(), which is documented as the one place that serialises them (:166-168); MainActor isolation is reentrant at await stream.send, so the two can overlap. The library likely serialises whole sends, making this ordering-only, but no primitive in this code enforces it. |  |  |  |  |
| AUDIT-0044 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/MLXEngine.swift:214-221,229-234 | unload() cannot cancel an in-flight load; the stale continuation installs the container afterwards | concurrency | OPEN | Node1.local | subagent-engine | unload() cancels loadingTask and nils it, but the load body's `do { let container = try await task.value; self.container = container; self.loadingTask = nil; onStateChange(.ready) }` still runs if the loader completes despite cancellation. stop() clears generationTask while the load group is parked in engine.load(), so setModel can rebuild and unload the old engine mid-load; the discarded engine keeps a checkpoint resident and posts .ready after being unloaded. |  |  |  |  |
| AUDIT-0045 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/MLXEngine+ReuseProbe.swift:31 | sessionReuseProbe drives container.perform without the MLX gate | concurrency | OPEN | Node1.local | subagent-engine | `let results = try await container.perform { ... }` (line 31) does not acquire MLXGate.shared, whose own doc (MLXGate.swift:8-20) says concurrent Metal evaluation aborts with EXC_BAD_ACCESS and that every Metal-touching operation is serialised through it. Only caller is the single-engine --session-probe, so it is latent. |  |  |  |  |
| AUDIT-0046 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/EngineService.swift:241-244 | removeAttachment validates a UUID case-insensitively then matches the uppercase string, refusing a lowercase id | logic | OPEN | Node1.local | subagent-engine | `guard UUID(uuidString: id) != nil` accepts a lowercased id, then `engine.attachments.filter { $0.id.uuidString != id }` compares against the uppercase uuidString and removes nothing, so the reply is 'there is no attached file with that id'. castVote (:284-289) parses to UUID and does it correctly. |  |  |  |  |
| AUDIT-0047 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/SystemProcess.swift:47-49 | SystemProcess children inherit the whole environment, including API keys | logic | OPEN | Node1.local | subagent-engine | Only executableURL and arguments are set (lines 47-49); environment stays nil, so textutil and openssl inherit TAVILY_API_KEY/DEEPSEEK_API_KEY/CHATBOTS_TRACE_API. Arguments are an array with fixed absolute executables and no shell, and the children are trusted Apple binaries, so the impact is negligible by itself. |  |  |  |  |
| AUDIT-0048 | S3 | A | ChatBotsCore | Sources/ChatBotsCore/Engine/TurnLoop.swift:208-222 | Loop-detection break rounds skips assembler.finish(), dropping up to 7 held-back characters | encoding | OPEN | Node1.local | subagent-engine | `if repetition.ingest(step.answer) { loopDetected = true; break rounds }` (lines 208-211) leaves the labelled loop, so `let tail = assembler.finish()` (line 222) never runs; ThinkingStripper holds up to endDelimiter.count - 1 = 7 characters as a possible partial delimiter (ThinkingStripper.swift:170-177) and they are lost from the answer. |  |  |  |  |
| AUDIT-0063 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:81-82,245 | start() guards only .running, so a concurrent start can launch a second engine on the same port | process-spawn | OPEN | Node1.local | subagent-app | `if case .running = state { return }` does not guard .starting, and launch() ends with self.process = process (line 245), overwriting any earlier child. A second start() while the first is still in its cancellation-blind 90 s loop can start a rival engine; the file's own comment (lines 266-271) records that failure mode. |  |  |  |  |
| AUDIT-0064 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:151-165 | terminateChild early-returns on a nil process, leaking the log file handle after a failed launch | resource-leak | OPEN | Node1.local | subagent-app | launch() opens and assigns logHandle (lines 238-242) before process.run() (line 244). If run() throws, self.process stays nil, terminateChild's `guard let process else { return }` returns before the `try? logHandle?.close(); logHandle = nil` at lines 163-164, so one descriptor leaks per failed launch. |  |  |  |  |
| AUDIT-0065 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/EngineSupervisor.swift:312-316 | The whole never-rotated engine log is read into memory on the MainActor | resource-leak | OPEN | Node1.local | subagent-app | tailOfLog does `try? String(contentsOf: logURL, encoding: .utf8)` on the full file. The log is opened append-only and nothing rotates or truncates app-engine.log; it is created 0644 under ~/Library/Application Support/ChatBots and, with CHATBOTS_TRACE_API set, also carries conversation content. |  |  |  |  |
| AUDIT-0066 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/ChatBotsApp.swift:127; ChatControllerConnection.swift:103-110 | Closing the window shuts the engine down but never disconnects the controller; the poll task survives | concurrency | OPEN | Node1.local | subagent-app | `onDisappear { Task { await supervisor.shutdown() } }` and nothing calls controller.disconnect(). The QUIC session and the 1 s poll task stay alive against the killed engine; the loop only exits when client.readerError is set, so a connection that merely times out keeps a 1 s failing poll, each send up to 10 s, for the life of the app process. |  |  |  |  |
| AUDIT-0067 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/ZoomStore.swift:31-42; Sources/ChatBotsCore/Support/TextZoom.swift:59 | The persisted text scale is trusted on read; a corrupted preference traps in Int(...) | logic | OPEN | Node1.local | subagent-app | `@AppStorage("textScale") private var storedScale: Double = TextZoom.default` with `get { storedScale }` and a clamping setter. TextZoom.percent is `Int((scale * 100).rounded())`, which traps for NaN or a value outside Int's range, and minimumWindowSize feeds the raw scale into NSSize. The app never writes an out-of-range value itself, so the trigger is a corrupted preference. |  |  |  |  |
| AUDIT-0068 | S3 | A | ChatBotsApp | Sources/ChatBotsApp/APIEndpointStore.swift:84-88,166-181 | Keychain write failures are swallowed and deleteKey has no caller | secret-handling | OPEN | Node1.local | subagent-app | setKey calls `Self.storeKey(value, seat: seat)` and discards the @discardableResult Bool from SecItemAdd; storeKey first deletes the old item (line 171), so a failed add silently destroys the previous key while the sheet still shows the new one as configured. static func deleteKey(seat:) (line 179) is never called. |  |  |  |  |

