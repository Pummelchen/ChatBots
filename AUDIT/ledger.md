# ChatBots audit — open work

Generated from `AUDIT/ledger.json` by `AUDIT/render-ledger.py`; edit the JSON, not this file.
**97 recorded · 92 closed · 1 open · 4 awaiting a decision**

Closed tasks stay in the JSON as the audit record and are not repeated here.

## Open

| # | task | sev | tier | project | title | where | status | what remains |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | AUDIT-0029 | S2 | A | repo | SwiftLint and swift-format are enforced against recorded waiver counts, not --strict, and force_unwrapping is never enabled | tools/mac-checks.sh:172-207; tools/analysis-waivers.txt | OPEN | SCOPE SPLIT (audit round 3): the task as recorded was three jobs in one — enabling `force_unwrapping`, driving SwiftLint to zero so `--strict` can be turned on, and the same for swift-format. The first is AUDIT-0097 (DONE); the SwiftLint-zero sweep is AUDIT-0098 (line_length), AUDIT-0099 (complexity and body length refactors) and AUDIT-0100 (the remaining rule classes); the swift-format sweep is AUDIT-0101 and the switch to `--strict` plus deleting the waiver counts is AUDIT-0102. This record stays OPEN until those are done — the scope was split, not narrowed. |

## Awaiting a decision

| # | task | sev | title | owner | situation | options |
| --- | --- | --- | --- | --- | --- | --- |
| 2 | AUDIT-0018 | S2 | The whole /api surface is unauthenticated and, through Caddy, reachable from the LAN by design | repository owner (Pummelchen) | /api has no authentication and the shipped Caddy default binds every interface; SECURITY.md and AGENTS.md both state this is deliberate for the phone-on-the-LAN use case, so removing it is a product decision rather than a code defect the audit can resolve alone. TRIED: read SECURITY.md, AGENTS.md, Caddyfile and tools/start.sh; confirmed crossOriginRefusal blocks cross-origin browser requests and requires application/json, confirmed there is no authn on any route, and confirmed --local-only inserts `bind 127.0.0.1`. | (1) sign off the documented LAN-trust model and record it as an accepted risk; (2) add a shared-secret bearer token (or password) to state-changing routes and make loopback the default bind, updating the web page, the SwiftUI app, the CLI and the probe to send it — a cross-project contract change with four consumers. |
| 3 | AUDIT-0058 | S2 | The app adopts any process answering on 7790 without verifying it, then hands it every Keychain API key | repository owner (Pummelchen) | the app adopts any process answering on 127.0.0.1:7790 and then sends it every Keychain API key; the client transport accepts a self-signed loopback peer without fingerprint enforcement, so a same-user process can impersonate the engine. Fixing it needs a trust mechanism between the app and the engine, which is a new cross-project contract (two products, one secret). TRIED: reviewed EngineSupervisor, WebTransportClient and SECURITY.md; confirmed the probe uses .localDevelopmentSelfSigned and that applyAPIEndpoints sends the keys. | (1) the engine writes a random session token to .run/ (0600) at startup and the app must see it echoed before it sends any key; (2) the app never adopts an existing engine and always launches its own, refusing to start when the port is taken. Both change the app/engine contract and need the owner's choice. |
| 4 | AUDIT-0071 | S2 | The same-origin check compares Origin against Host/X-Forwarded-Host, so DNS rebinding defeats it | repository owner (Pummelchen) | the same-origin check compares Origin against Host/X-Forwarded-Host, which a DNS-rebinding page satisfies because both are the attacker's own name; the defense needs either authentication (AUDIT-0018's decision) or a Host allow-list of the names this deployment is legitimately served under, and the latter is not derivable without owner input — Caddy serves any Host, and a legitimate remote-access name (a VPN name, say) must not be broken. TRIED: read APIServer.crossOriginRefusal, the Caddyfile and tools/start.sh; confirmed the equality check and the application/json requirement, and that Sec-Fetch-Site is same-origin after a rebind. | (1) resolve AUDIT-0018 by adding authentication, which subsumes this; (2) configure an explicit Host allow-list (loopback/LAN IPs, .local names, plus any owner-named remote host) and refuse others; (3) accept the risk explicitly as part of the LAN-trust sign-off. |
| 5 | AUDIT-0077 | S2 | The shipped default publishes the unauthenticated site and API on every interface over plaintext HTTP | repository owner (Pummelchen) | the same deliberate LAN-trust decision as AUDIT-0018, seen from the Caddy side — Caddyfile:40 carries no bind, so the unauthenticated site and API are published on every interface, over plaintext HTTP, unless --local-only is passed. TRIED: read the Caddyfile, tools/start.sh (default LOCAL_ONLY=0) and SECURITY.md; start.sh already prints the exposure. | (1) accept and sign off; (2) make the default Caddyfile bind 127.0.0.1 and require an explicit --lan flag to publish, adding TLS for the LAN case. |

